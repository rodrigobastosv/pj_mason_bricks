name: page_generator
description: A brick to create a page with it's basic structure

version: 0.1.0+1

environment:
  mason: ">=0.1.0-dev.60 <0.1.0"

vars:
  name:
    type: string
    description: The name of your page
    prompt: What is the page name?
